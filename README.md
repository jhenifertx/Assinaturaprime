
  # assinatura-pc (Community)

  This is a code bundle for assinatura-pc (Community). The original project is available at https://www.figma.com/design/gF2OkK08s5fdb6dFFljz62/assinatura-pc--Community-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  